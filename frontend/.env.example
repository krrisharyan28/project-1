# Copy to .env. Vite only exposes vars prefixed with VITE_.

# Base URL for API calls. Leave as /api to use the Vite dev proxy (recommended
# in development). Set to a full URL (e.g. http://localhost:8000/api) if not
# proxying.
VITE_API_BASE_URL=/api
